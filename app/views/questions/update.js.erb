$('.form-errors[data-form-id="<%= @question.id %>"]').html('<%= render 'shared/errors', resource: @question %>');

<% unless @question.errors.present? %>
$('.question[data-question-id="<%= @question.id %>"]').replaceWith('<%= j render 'question_part', resource: @question %>');
<% end %>
